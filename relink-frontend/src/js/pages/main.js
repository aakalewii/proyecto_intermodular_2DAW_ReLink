// src/js/pages/main.js

import { renderNavbar } from '../components/Navbar.js';


document.addEventListener('DOMContentLoaded', () => {
    // Pintamos la barra de navegación nada más cargar la página
    renderNavbar();
    
    // (Más adelante, aquí pondremos la función de cargarAnuncios())
});